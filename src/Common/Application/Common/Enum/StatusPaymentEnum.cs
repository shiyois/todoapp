namespace CleanApplication.Application.Common.Enum
{
    public enum OrderStatus
    {
        /// <summary>
        /// 0=New, 1=Canceled, 2=Payment Pending, 3=Success
        /// </summary>
        New,
        Canceled,
        Pending,
        Success
    }
}
